[project]
name = "backend"
version = "0.1.0"
description = "Backend for the kohyli e-commerce website"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "sqlmodel>=0.0.24",
]
